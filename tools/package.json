{
  "name": "tools",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build -w slack-cli && npm run build -w confluence-cli && npm run build -w jira-cli",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^9.16.0",
    "@types/minimist": "^1.2.5",
    "@types/node": "^22.10.1",
    "dotenv": "^17.3.1",
    "eslint": "^9.16.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.16.0"
  }
}
