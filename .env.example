# OpenClaw + Ollama (Docker) 用 .env
# コピー: cp .env.example .env のあと、必要に応じて編集

# Gateway 認証トークン（必須・推奨: openssl rand -hex 32 で生成）
OPENCLAW_GATEWAY_TOKEN=change-me-to-a-long-random-token

# Ollama 用（ローカルなので任意の値で可）
OLLAMA_API_KEY=ollama-local

# 設定・ワークスペース一式のホスト側パス（config/ に openclaw.json, agents/, workspace/, workspace-test/ などを含む）
OPENCLAW_CONFIG_DIR=./config

# ポート（必要なら変更）
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_BRIDGE_PORT=18790

# バインド（lan = 同一ネットワークからアクセス可）
OPENCLAW_GATEWAY_BIND=all

# 公式イメージのタグ（main / 2026.2.24 など）
# OPENCLAW_IMAGE=ghcr.io/openclaw/openclaw:main

# --- スキル用（読み取り・書き込みで利用。値は .env に記載し Git にコミットしないこと）
# Slack（channels.slack / slack スキル）
# SLACK_BOT_TOKEN=xoxb-...

# GitHub（github スキル・gh CLI）
# GH_TOKEN=ghp_...

# Confluence（confluence-read スキル）
# CONFLUENCE_BASE_URL=https://your-domain.atlassian.net/wiki
# CONFLUENCE_USER=you@example.com
# CONFLUENCE_TOKEN=...

# Jira（jira-read スキル）
# JIRA_BASE_URL=https://your-domain.atlassian.net
# JIRA_USER=you@example.com
# JIRA_TOKEN=...
